Act as a technical editor. Reformat the following outline into a clean Markdown document hierarchy using ATX-style headings (e.g., #, ##, ###).

Follow these specific rules:

- Use a single # Heading 1 for the main title.
- Use ## Heading 2 for major sections.
- Use ### Heading 3 for sub-points.
- Do not skip heading levels (e.g., don't jump from # to ###).
- Ensure there is a single space after the '#' symbols and a blank line between every heading.
- Convert any nested bullet points under a sub-heading into a flat bulleted list using dashes (-).